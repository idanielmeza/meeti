<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://necolas.github.io/normalize.css/8.0.1/normalize.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Raleway:wght@400;700;900&family=Staatliches&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/app.css">
    <title>Meeti - <%- nombrePagina %></title>
</head>
<body>
    
    <header class="site-header contenedor">
        <a href="/">
            <img src="/img/logo.png" alt="Meeti">
        </a>

        <nav class="nav">
            <a href="/">Crear Grupo</a>
            <a href="/auth/iniciar-sesion">Iniciar Sesion</a>
            <a href="/auth/crear-cuenta">Registrarse</a>
        </nav>

    </header>

    <% if(locals.mensajes) { %>
        <% const categoria = Object.keys(locals.mensajes) %>
        <% if(categoria.length) { %>
            <% locals.mensajes[categoria].forEach(alerta => { %>
                <div class="alertas">
                    <p class='alerta <%= categoria%>'> <%= alerta%> </p>
                </div>
            <% }) %>
        <% } %>
    <% } %>

    <%- body %>

    <footer class="site-footer contenedor">
        <nav class="nav">
            <a href="/">Crear Grupo</a>
            <a href="/auth/iniciar-sesion">Iniciar Sesion</a>
            <a href="/auth/crear-cuenta">Registrarse</a>
        </nav>

        <p class="copyright">Todos los derechos resevrados Meeti <%- new Date().getFullYear() %> &copy</p>

    </footer>

</body>
</html>